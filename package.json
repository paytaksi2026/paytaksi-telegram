{
  "name": "paytaksi-v2-enterprise-3bot",
  "private": true,
  "version": "2.0.0",
  "description": "PayTaksi v2 Enterprise (Passenger + Driver + Admin) — Render-ready mono repo runner",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "postinstall": "npm --prefix backend install && npm --prefix bots install",
    "start": "npx concurrently -k -n BACKEND,BOTS -c auto \"npm --prefix backend start\" \"npm --prefix bots start\"",
    "start:backend": "npm --prefix backend start",
    "start:bots": "npm --prefix bots start"
  },
  "dependencies": {
    "concurrently": "^9.0.1"
  }
}
